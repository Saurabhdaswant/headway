// form handling and validation
// - show error if empty inputs / not selected any value which is required
// hover on submit button
    // html, csss, react, redux, stylecompoent, routing, charting, typscript
